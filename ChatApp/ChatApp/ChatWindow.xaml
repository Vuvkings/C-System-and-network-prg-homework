<Window x:Class="ChatApp.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatApp"
        mc:Ignorable="d"
        Title="ChatWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox
            x:Name="MessagesListBox"
            Grid.Row="0"
            Margin="0,0,0,10" />

        <ScrollViewer Grid.Row="1" Height="55">
            <TextBox
                x:Name="MessageInputTextBox"
                Margin="0,0,0,5"
                AcceptsReturn="True"
                PreviewKeyDown="MessageInputTextBox_PreviewKeyDown"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                TextWrapping="Wrap" />
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <ComboBox
                x:Name="RecipientComboBox"
                Width="120"
                Margin="0,0,10,0" />
            <Button Click="OnSendMessageClick" Content="Отправить" />
        </StackPanel>
    </Grid>
</Window>
